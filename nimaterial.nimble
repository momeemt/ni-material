version       = "0.3.0"
author        = "Momeemt"
description   = "nimaterial is a CSS output library based on material design."
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 1.0.6"
requires "Palette >= 0.1.0"